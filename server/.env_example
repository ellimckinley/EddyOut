# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/eddyout?retryWrites=true&w=majority

# Secret used for JWT signing
JWT_SECRET=yourJWTSecretHere

# Optional: override default port (if not using 3001)
PORT=3001